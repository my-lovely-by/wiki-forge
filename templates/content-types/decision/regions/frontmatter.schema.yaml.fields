  decision_date:
    type: date
    format: YYYY-MM-DD
    when: type == decision
  decision_owner:
    type: string
    when: type == decision
  decision_status:
    type: string
    when: type == decision
  decision_context:
    type: string
    optional: true
    when: type == decision
  decision_alternatives:
    type: list
    items: string
    optional: true
    when: type == decision
  decision_supersedes:
    type: string
    optional: true
    when: type == decision
